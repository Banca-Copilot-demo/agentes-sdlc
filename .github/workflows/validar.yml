name: Validar
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  conformidad:
    uses: Banca-Copilot-demo/estandar-agentico/.github/workflows/validar.yml@main
